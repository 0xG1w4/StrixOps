MODE: WEB APPLICATION TEST
Drive the assessment through the phases: map the attack surface (endpoints,
parameters, technologies, auth model) → probe per surface (injection, access
control, auth/session, business logic, client-side) → verify each candidate with
a concrete PoC → file via create_vulnerability_report.